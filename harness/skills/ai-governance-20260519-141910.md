```markdown
---
name: daily-news-workflow
description: 从7类新闻源采集→去重→8段日报生成→Prophet信号→Obsidian vault 的完整每日工作流，含 arXiv 论文解读
tags: [ai-governance, data-compliance, news-workflow]
version: 1.1.0
auto_generated: true
harness_confidence: 0.54
---

# Daily News Workflow

## When to Use
当用户请求“今日新闻”、“每日日报”或需要全自动中文信息采集与报告时触发。覆盖 7 大类新闻源，生成 8 段标准化日报，输出 Prophet 信号并同步至 Obsidian vault。

## Updated Guidance (from session session_20260519141755)
**Explicit correction detected** — 用户指出此前谓词链存在两个缺口，且 arXiv WebFetch 连续失败需要修复。  
**两个缺口已补入谓词链：**  
1. **算力网政策监控缺口** — 在新闻分类中增加“算力网”政策专项采集，确保相关动态不漏。  
2. **arXiv 采集可靠性缺口** — 原流程中 arXiv 使用默认 WebFetch 经常被拦截，需强制启用 `skipWebFetchPreflight: true` 避免校验失败。  

同时明确：所有 WebFetch 调用若被全局拦截（非 arXiv 专用场景），则在整个新闻流程中不再调用，改用备用源或跳过。

## How To
1. **谓词链完整版**（7 步）：  
   信息源扫描（含算力网专项） → 去重 → 主题分类 → 重要性排序 → 8 段日报撰写 → Prophet 信号计算 → Obsidian vault 写入。  
2. **算力网政策专项**：在“政策/监管”类源中新增关键词 `算力网` `全国一体化算力网络` `东数西算`，设置单独采集规则，避免与通用 IT 新闻混淆。  
3. **arXiv WebFetch 强制配置**：  
   - 在 `settings.local.json` 中为 arXiv 域设置 `skipWebFetchPreflight: true`。  
   - 调用时使用 `--skip-preflight` 或配置预检跳过，确保 PDF/摘要页可直接抓取。  
   - 若虽然启用仍失败，回退为仅提取 API 元数据，不尝试抓取全文。  
4. **全局 WebFetch 避险**：  
   若检测到任意 WebFetch 调用均返回 403/503 或空白，则本轮日报无该源，并在风险备注中标注“部分源因拦截中断”。  
5. **设计预留升级路径**：当前使用文件系统存储日记，但模块化设计已为未来引入向量数据库留下接口（`storage/vector_db_adapter.py`）——Phase 4 可平滑切换，无需改动上游流程。  
6. **Harness 联动**（可选）：Stop Hook 中配置清理脚本，杀掉遗留 MCP 僵尸进程，保障下一次工作流启动时环境干净。
```